# v0.1.1 Release Notes

## Highlights

- 重做 Unity Editor window，让新用户从 Start tab 按步骤接入。
- 增加 Tables、Merge、Gate、Output tabs。
- 增加重要控件 tooltip 和 copy-command 按钮。
- 增加本地 config/registry 状态行。
- 改进 Unity 侧 Windows PATH/PATHEXT CLI 解析。

## 安装

Unity Package Manager Git URL：

```text
https://github.com/today080221/config-sheet-forge.git?path=/packages/unity#v0.1.1
```
